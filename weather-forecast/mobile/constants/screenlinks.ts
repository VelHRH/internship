const enum ScreenLink {
  LOGIN = "/login",
  FORECAST_WEATHER = "/forecast",
  SIGN_UP = "/sign-up",
  MAIN = "/",
  CURRENT_WEATHER = "/current",
  SEARCH_RESULTS = "/search-results",
}

export default ScreenLink;
