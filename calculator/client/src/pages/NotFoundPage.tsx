import { FC } from 'react';

import ErrorElement from 'components/common/ErrorElement';

const NotFoundPage: FC = () => {
  return <ErrorElement>Page not found :(</ErrorElement>;
};

export default NotFoundPage;
